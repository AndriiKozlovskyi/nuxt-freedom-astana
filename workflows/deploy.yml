name: Build and Deploy Branch

on:
  push:
    branches:
      - main
  workflow_dispatch:
  repository_dispatch:
    types: [storyblok-publish]
jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout main
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm ci

      - name: Generate static site
        run: npm run generate

      - name: Deploy to deploy branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: deploy
          publish_dir: .output/public
          force_orphan: true

      - name: Notify Plesk to pull latest deploy
        run: |
          curl -s -X POST "https://srv-plesk63.ps.kz:8443/modules/git/public/web-hook.php?uuid=fa6bd23a-8438-f336-e28c-0e1f0e5bde04"